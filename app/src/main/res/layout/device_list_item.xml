<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- RIGA 1 (COLLAPSATO IN SCANSIONE): Nome + Tag BLE (Solo per BLE) + Tasto Target -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/nameText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Sconosciuto"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/text_primary"
                android:ellipsize="end"
                android:maxLines="1" />

            <!-- TAG SINTETICO SOLO PER BLE (Nessun tag per Classico) -->
            <TextView
                android:id="@+id/technologyBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="BLE"
                android:textSize="10sp"
                android:textStyle="bold"
                android:paddingStart="6dp"
                android:paddingEnd="6dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:background="@drawable/bg_badge_ble"
                android:textColor="#1E3A8A"
                android:layout_marginEnd="6dp" />

            <!-- Pulsante Target Diretto -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/selectButton"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="TARGET"
                android:textSize="10sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                app:backgroundTint="#3F51B5"
                app:cornerRadius="6dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:paddingStart="12dp"
                android:paddingEnd="12dp" />

            <TextView
                android:id="@+id/expandArrow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="▼"
                android:textSize="10sp"
                android:textColor="@color/text_secondary"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- RIGA 2 (IN SCANSIONE): Indirizzo MAC + Distanza stimata in Metri + RSSI -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="2dp">

            <View
                android:id="@+id/rssiDot"
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:background="@drawable/dot_indicator"
                android:backgroundTint="@color/target_active_green" />

            <TextView
                android:id="@+id/rssiText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="FC:58:FA:82:11:4E • ~1.5 m (-72 dBm)"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/text_secondary" />
        </LinearLayout>

        <!-- DETTAGLI ESPANSI AL TOCCO -->
        <LinearLayout
            android:id="@+id/expandedDetailContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:layout_marginTop="12dp"
            android:paddingTop="12dp"
            android:background="#F8FAFC"
            android:padding="12dp">

            <!-- Tipologia Identificata Reale -->
            <TextView
                android:id="@+id/typeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tracker / Portachiavi (iTAG) (98%)"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="#3F51B5"
                android:background="@drawable/bg_chip_selected"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp" />

            <!-- Produttore Identificato -->
            <TextView
                android:id="@+id/manufacturerText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Produttore: Nordic Semiconductor"
                android:textSize="11sp"
                android:textColor="@color/text_secondary"
                android:layout_marginTop="6dp" />

            <!-- Servizi e UUID -->
            <TextView
                android:id="@+id/uuidText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Servizi: Immediate Alert, Link Loss"
                android:textSize="10sp"
                android:textColor="@color/text_secondary"
                android:layout_marginTop="4dp" />

            <!-- Fast Pair Model ID se presente -->
            <TextView
                android:id="@+id/modelIdText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Fast Pair Model ID: N/D"
                android:textSize="10sp"
                android:textColor="#1976D2"
                android:visibility="gone"
                android:layout_marginTop="2dp" />

            <!-- Azioni Avanzate Espanse -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="end"
                android:layout_marginTop="10dp">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/renameButton"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:text="Rinomina"
                    android:textSize="10sp"
                    android:textColor="@color/text_primary"
                    app:cornerRadius="6dp"
                    android:layout_marginEnd="6dp"
                    android:insetTop="0dp"
                    android:insetBottom="0dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/inspectButton"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:text="INTERROGA"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:textColor="#3F51B5"
                    app:strokeColor="#3F51B5"
                    app:strokeWidth="1dp"
                    app:backgroundTint="#E0E7FF"
                    app:cornerRadius="6dp"
                    android:insetTop="0dp"
                    android:insetBottom="0dp"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp" />
            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E5E7EB"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="-14dp"/>

</LinearLayout>
